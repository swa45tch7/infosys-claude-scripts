---
description: Show one LogicMonitor device by id or name (read-only)
---

Run from the repository root. Replace IDENTIFIER with the device id or name.

```bash
python3 -m lm_infosys device "IDENTIFIER"
```

Summarize status and collector. If several names match, list the matches and ask which id to use.
