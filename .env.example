# Copy to .env (never commit .env) or export these in the shell before asking Claude.
# Create the API token in LogicMonitor: Settings → Users & Roles → API Tokens.

LM_ACCOUNT=yourcompany
LM_ACCESS_ID=
LM_ACCESS_KEY=

# Optional. Default is logicmonitor.com
# LM_DOMAIN=logicmonitor.com
